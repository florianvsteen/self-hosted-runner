version: '3.8'
services:
  runner:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - REPO=florianvsteen/lightweightchart-agent
      - REG_TOKEN=BNDGSDBO7B6TUNJXV4FX4YTJ32UWG
      - NAME=FLOWNET-GH-RUNNER
    deploy:
      mode: replicated
      replicas: 2
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.4'
          memory: 256M
