version: '3.8'
services:
  runner:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    privileged: true
    environment:
      - REPO=florianvsteen/lightweightchart-agent
      - REG_TOKEN=BNDGSDABO3O3NTXSAEHRMVDKHLITM
      - NAME=FLOWNET-GH-RUN
    deploy:
      mode: replicated
      replicas: 1
      #resources:
      #  limits:
      #    cpus: '0.5'
      #    memory: 512M
      #  reservations:
      #    cpus: '0.4'
      #    memory: 256M
